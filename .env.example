# .env.example
# Copy this to .env and fill in your values for local testing
# NOTE: For production deployment via GitHub Actions, use Secret Manager instead

# ===== Google Cloud Settings =====
export GCP_PROJECT_ID="your-project-id"
export GCP_REGION="us-central1"
export DOMAIN="mesh.example.com"
export MESHCENTRAL_VERSION="1.1.56"

# ===== Cloudflare Tunnel =====
# Get this token from: cloudflared tunnel token YOUR_TUNNEL_NAME
export CLOUDFLARE_TOKEN="eyJh..."

# ===== MongoDB Connection =====
# MongoDB Atlas connection string (free tier available)
export MONGO_URL="mongodb+srv://user:password@cluster0.xxxxx.mongodb.net/meshcentral?authSource=admin"

# ===== OIDC Authentication (Optional) =====
# Auth0, Google, Okta, or any OpenID Connect provider
# export OIDC_CLIENT_ID="your-client-id"
# export OIDC_CLIENT_SECRET="your-client-secret"
# export OIDC_ISSUER="https://your-tenant.auth0.com"

# ===== Notes =====
# - For GitHub Actions deployment, configure these values in:
#   1. .github/workflows/deploy.yml (for GCP_PROJECT_ID, REGION, DOMAIN, MESHCENTRAL_VERSION)
#   2. Secret Manager (for MONGO_URL, CLOUDFLARE_TOKEN, OIDC credentials)
#
# - This .env file is only used for local testing with deploy.sh (deprecated)
#
# - Never commit .env to git! (.gitignore already includes it)
